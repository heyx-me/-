# Quick Task 004: Fix Nanie Auth

**Goal:** Fix Supabase connection in Nanie app by providing valid credentials.

## Plan
1.  Create `nanie/config.js` with valid `SUPABASE_URL` and `SUPABASE_KEY`.
2.  Update `nanie/app.jsx` to import config.
3.  Commit.
