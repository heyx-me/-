# Phase 3 Plan: State & Components

## Goal
Verify React state management, Agent logic, and UI component rendering.

## Requirements
- [ ] **UNIT-04**: `rafi/hooks/useBanking.jsx` (via `BankingContext.jsx`) logic is tested.
- [ ] **UNIT-05**: `rafi/components/Showcase.jsx` renders correctly with mock data.
- [ ] **INT-01**: `RafiAgent` state machine transitions verified.
- [ ] **INT-02**: Agent handles invalid messages gracefully.

## Implementation Steps
1.  **Banking Hook Tests**:
    -   Test initial state.
    -   Test `login` transition.
    -   Test transaction loading state.
2.  **Component Tests**:
    -   Test `Showcase` renders transactions.
    -   Test `Header` renders user info or login button.
3.  **Agent Tests**:
    -   Mock `fetch` and Agent responses.
    -   Test state transitions (INIT -> STATUS -> DATA).

## Verification
- Run `npm test` and ensure all tests pass.
