# Phase 8: UI: Collaboration - Implementation Plan

## Goal
Enable sharing threads via links, joining conversations, and viewing participants.

## 1. Deep Linking & Joining
- [ ] **ID-01**: Join Logic in `ConversationContext`
  - Add `joinConversation(threadId)` function.
  - In `init` logic:
    - If `thread` ID is in URL but user is not a member:
    - Set `needsToJoin` state.
- [ ] **UI-01**: Join Screen
  - Create a `JoinThread` overlay in `app.jsx`.
  - Display thread title and "Join this conversation?" button.

## 2. Participants UI
- [ ] **UI-02**: Participant List in Header
  - Update `AddressBar` or add a new component in the header to show avatars of `conversation_members`.
  - Use initials or colors for participants (since we don't have profile pics).
- [ ] **UI-03**: Share Button
  - Add a "Share" button to the active thread (in sidebar or header).
  - Copies `/?thread=<UUID>` to clipboard.

## 3. Polish & Verification
- [ ] **SEC-01**: Update RLS (Optional/Future)
  - Ensure `messages` are only visible to `members`.
- [ ] **VER-01**: Test Join Flow (Open link in new browser/incognito -> Join -> Chat works).
- [ ] **VER-02**: Test Participant List (Verify multi-user avatars appear).
