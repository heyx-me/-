# Phase 12 Plan: Simplify Main Page & Polish

## 1. Component Refactoring: AppList
- [ ] **Create `AppList`**:
    - Props: `apps`, `conversations`, `currentId`, `onSelect`.
    - **Logic:**
        - Map through `apps`.
        - Find matching `conversation` for each app (by `app_id`).
        - Render item with App Icon, Name, and (if conv exists) Last Message + Time.
- [ ] **Remove Obsolete Components**:
    - `SidebarHeader`
    - `SidebarTabs`
    - `ContactsList`
    - `ConversationList` (merged logic).

## 2. Main Layout Update
- [ ] **Update `App` return**:
    - Sidebar column contains *only* `AppList` (full height).
    - Remove the top header/tabs area.

## 3. Styling Polish
- [ ] **Chat Bubbles (`MessageBubble`)**:
    - **User:** `bg-blue-600 text-white rounded-2xl rounded-tr-sm`.
    - **Bot:** `bg-zinc-800 text-zinc-200 rounded-2xl rounded-tl-sm`.
    - **Spacing:** Increase gap between messages.
    - **Alignment:** Ensure max-width is reasonable (e.g., 75-80%).
- [ ] **Last Message Format**:
    - Helper function `formatTime(date)`:
        - If today: HH:MM
        - If yesterday: "Yesterday"
        - Else: DD/MM/YY

## 4. Verification
- [ ] **Sidebar:** Verify it shows all apps.
- [ ] **Interaction:** Clicking an app opens the thread.
- [ ] **Visuals:** Verify bubbles look better and layout is clean.
